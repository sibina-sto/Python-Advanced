def get_unique_compounds(n):
    all_elements = set()

    for _ in range(n):
        compounds = set(input().split())
        all_elements |= compounds

    return all_elements


unique_chemical_compounds = get_unique_compounds(int(input()))
print('\n'.join(unique_chemical_compounds))

def get_phonebook_info():
    phonebook = {}

    entry = input()
    while not entry.isdigit():
        name, number = entry.split("-")
        phonebook[name] = number

        entry = input()

    n = int(entry)

    return phonebook, n

